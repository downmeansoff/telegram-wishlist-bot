# 🌐 Настройка HTTPS для Web App

## 🚀 САМЫЙ ПРОСТОЙ СПОСОБ (2 минуты)

### Вариант 1: Автоматический скрипт

Просто **дважды кликните** на файл:
```
setup-ngrok.bat
```

Скрипт:
1. Откроет страницу загрузки ngrok
2. Попросит распаковать ngrok.exe в папку проекта
3. Запустит ngrok
4. Автоматически обновит .env
5. Перезапустит бота

**Готово!** Web App заработает! 🎉

---

### Вариант 2: Ручная настройка

#### Шаг 1: Скачайте ngrok
1. Откройте: https://ngrok.com/download
2. Нажмите **"Download for Windows"**
3. Распакуйте `ngrok.exe`

#### Шаг 2: Запустите ngrok
1. Откройте **Command Prompt** или **PowerShell**
2. Перейдите в папку с ngrok
3. Запустите:
```bash
ngrok http 3000
```

4. Вы увидите:
```
Forwarding    https://abc123.ngrok-free.app -> http://localhost:3000
```

5. **Скопируйте HTTPS URL** (например: `https://abc123.ngrok-free.app`)

#### Шаг 3: Обновите .env

Откройте файл `.env` и замените строку:

**Было:**
```env
WEB_APP_URL=http://localhost:3000
```

**Стало:**
```env
WEB_APP_URL=https://abc123.ngrok-free.app
```
(Вставьте ваш URL из ngrok)

Сохраните файл.

#### Шаг 4: Перезапустите бота

```bash
docker-compose restart bot
```

#### Шаг 5: Готово!

1. Откройте Telegram
2. Найдите [@iiiwishlistbot](https://t.me/iiiwishlistbot)
3. Отправьте `/start`
4. **Увидите кнопку "🎁 Мои желания"**
5. Нажмите - откроется Web App!

---

## ⚠️ Важно знать про ngrok

### ✅ Плюсы:
- Бесплатный
- Работает из коробки
- HTTPS включен
- Идеально для разработки

### ⚠️ Особенности:
- URL меняется при каждом перезапуске ngrok
- Есть страница "Visit Site" перед загрузкой (можно пропустить)
- Нужно держать окно ngrok открытым

### 🔄 Если перезапустили ngrok:
1. Получите новый URL
2. Обновите `WEB_APP_URL` в `.env`
3. Перезапустите бота: `docker-compose restart bot`

### 💡 Для постоянного URL:
Зарегистрируйтесь на https://ngrok.com (бесплатно):
1. Получите authtoken
2. Запустите: `ngrok authtoken YOUR_TOKEN`
3. Теперь можно использовать: `ngrok http 3000 --domain=your-domain.ngrok-free.app`

---

## 🚀 Альтернатива: Деплой на продакшн (10 минут)

Если хотите постоянный HTTPS без ngrok:

### Frontend на Vercel (бесплатно):

1. Зарегистрируйтесь: https://vercel.com
2. Установите Vercel CLI:
```bash
npm install -g vercel
```

3. Задеплойте frontend:
```bash
cd frontend
vercel --prod
```

4. Получите URL: `https://your-app.vercel.app`

### Backend на Railway (бесплатно):

1. Зарегистрируйтесь: https://railway.app
2. Создайте новый проект
3. Добавьте PostgreSQL
4. Подключите GitHub репозиторий (папка backend)
5. Получите URL: `https://your-backend.railway.app`

### Обновите .env:

```env
WEB_APP_URL=https://your-app.vercel.app
API_URL=https://your-backend.railway.app
REACT_APP_API_URL=https://your-backend.railway.app
```

### Обновите frontend/.env:

```env
VITE_API_URL=https://your-backend.railway.app
```

### Перезапустите бота:

```bash
docker-compose restart bot
```

**Готово!** Теперь у вас постоянный HTTPS! 🎉

---

## ❓ FAQ

**Q: Ngrok не работает?**
- Проверьте, что порт 3000 свободен: `docker-compose ps`
- Перезапустите frontend: `docker-compose restart frontend`

**Q: Web App не открывается?**
- Проверьте URL в `.env` (должен начинаться с https://)
- Перезапустите бота: `docker-compose restart bot`
- Подождите 5-10 секунд

**Q: Ngrok показывает "Visit Site"?**
- Это нормально для бесплатной версии
- Просто нажмите "Visit Site"
- Или зарегистрируйтесь на ngrok.com для пропуска этого экрана

**Q: URL ngrok постоянно меняется?**
- Зарегистрируйтесь на ngrok.com
- Получите постоянный домен (бесплатно)
- Или задеплойте на Vercel

**Q: Лучше ngrok или деплой?**
- **Для тестирования:** ngrok (быстрее)
- **Для продакшна:** деплой на Vercel/Railway

---

## 🎯 Рекомендация

**Для начала:** Используйте `setup-ngrok.bat` - это самый быстрый способ!

Просто дважды кликните на файл и следуйте инструкциям. Через 2 минуты Web App будет работать! 🚀

---

**После настройки откройте бота:** [@iiiwishlistbot](https://t.me/iiiwishlistbot)
