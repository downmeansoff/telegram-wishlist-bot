# ⚡ Быстрый старт

## Шаг 1: Убедитесь, что Docker запущен

Запустите **Docker Desktop** и дождитесь, пока он полностью загрузится (иконка Docker в трее должна быть зеленой).

## Шаг 2: Запустите приложение

### Windows:
Просто дважды кликните на файл **`setup.bat`** в корне проекта.

### Linux/Mac:
```bash
chmod +x setup.sh
./setup.sh
```

### Или вручную:
```bash
# 1. Запустите контейнеры
docker-compose up -d

# 2. Примените миграции (подождите 10 секунд после запуска)
docker-compose exec backend alembic upgrade head

# 3. Проверьте статус
docker-compose ps
```

## Шаг 3: Откройте бота

1. Откройте Telegram
2. Найдите: [@iiiwishlistbot](https://t.me/iiiwishlistbot)
3. Нажмите `/start`
4. Нажмите кнопку **"🎁 Мои желания"**

## 🎉 Готово!

Приложение работает на:
- 🤖 **Telegram Bot**: https://t.me/iiiwishlistbot
- 🌐 **Web App**: http://localhost:3000
- 📡 **API**: http://localhost:8000
- 📚 **API Docs**: http://localhost:8000/docs

---

## 📊 Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f bot
docker-compose logs -f frontend

# Перезапуск
docker-compose restart

# Остановка
docker-compose stop

# Остановка + очистка
docker-compose down

# Остановка + очистка БД
docker-compose down -v
```

---

## ❓ Проблемы?

### Docker не запускается
- Убедитесь, что Docker Desktop установлен и запущен
- Перезапустите Docker Desktop

### "Port already in use"
```bash
# Остановите существующие контейнеры
docker-compose down

# Или освободите порты 3000, 8000, 5432, 6379
```

### База данных не создается
```bash
# Пересоздайте БД
docker-compose down -v
docker-compose up -d
# Подождите 10 секунд
docker-compose exec backend alembic upgrade head
```

### Бот не отвечает
1. Проверьте, что токен правильный в `.env`
2. Проверьте логи: `docker-compose logs bot`
3. Перезапустите: `docker-compose restart bot`

---

## 📖 Подробная документация

- [README.md](README.md) - Полная документация
- [SETUP.md](SETUP.md) - Детальная инструкция по установке
- [CONTRIBUTING.md](CONTRIBUTING.md) - Гайд для разработчиков
