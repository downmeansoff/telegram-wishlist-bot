# 🧪 Инструкция по тестированию

## Важно понять!

Это Telegram **Mini App** (Web App), который работает **только внутри Telegram**.

При открытии http://localhost:3000 в обычном браузере будет бесконечная загрузка, потому что приложение ждет данные от Telegram API, которых нет в обычном браузере.

---

## ✅ Правильный способ тестирования

### 1. Через Telegram Bot (Рекомендуется)

1. Откройте Telegram
2. Найдите бота: [@iiiwishlistbot](https://t.me/iiiwishlistbot)
3. Отправьте `/start`
4. Нажмите кнопку **"🌐 Открыть Web App"**
5. Приложение откроется **внутри Telegram**

**Примечание**: В режиме разработки (http://localhost:3000) кнопка откроет браузер, а не встроенный Web App, потому что Telegram требует HTTPS для Web App.

### 2. Эмуляция Telegram в браузере (Для разработки UI)

Если вы хотите тестировать в браузере без Telegram:

1. Откройте http://localhost:3000
2. Откройте Console (F12)
3. Вставьте этот код:

```javascript
window.Telegram = {
  WebApp: {
    initData: "",
    initDataUnsafe: {
      user: {
        id: 123456789,
        first_name: "Test",
        last_name: "User",
        username: "testuser",
        language_code: "ru"
      }
    },
    expand: () => console.log('expand'),
    ready: () => console.log('ready'),
    enableClosingConfirmation: () => {},
    setHeaderColor: () => {},
    colorScheme: "light",
    MainButton: {
      show: () => {},
      hide: () => {},
      setText: (text) => {},
      onClick: (fn) => {},
      offClick: (fn) => {},
    },
    BackButton: {
      show: () => {},
      hide: () => {},
      onClick: (fn) => {},
    }
  }
};
```

4. Перезагрузите страницу (F5)
5. Теперь приложение загрузится!

**Важно**: API запросы будут падать с ошибкой 401, потому что нет настоящего `initData` от Telegram. Это нормально для тестирования UI.

---

## 🔧 Тестирование Backend API

### Через Swagger UI (Без аутентификации Telegram)

1. Откройте: http://localhost:8000/docs
2. Все эндпоинты доступны для тестирования
3. Можно создавать желания, пользователей, группы

**Примечание**: Некоторые эндпоинты требуют заголовок `X-Telegram-Init-Data`, который можно пропустить для тестирования.

### Через curl

```bash
# Проверка здоровья
curl http://localhost:8000/health

# Получить список желаний (требует авторизацию)
curl http://localhost:8000/api/wishes
```

---

## 🤖 Тестирование Telegram Bot

### Команды бота

Откройте бота в Telegram и попробуйте:

```
/start - Главное меню
/add - Добавить желание
/list - Показать список (топ-5)
/share - Поделиться списком
/help - Справка
```

### Быстрое добавление желания

Просто напишите название желания боту (без команды):
```
iPhone 15 Pro
```

Бот добавит его в список!

---

## 🌐 Для продакшн деплоя

Когда вы задеплоите приложение на HTTPS:

1. Обновите `.env`:
```env
WEB_APP_URL=https://your-domain.com
TELEGRAM_BOT_WEBHOOK_URL=https://your-domain.com/api/webhook
```

2. Перезапустите:
```bash
docker-compose restart bot backend
```

3. Теперь в боте будет **настоящая Web App кнопка**:
   - Откроется внутри Telegram (не в браузере)
   - Будет работать на iOS и Android
   - Полная интеграция с Telegram

---

## 📊 Проверка логов

### Все сервисы
```bash
docker-compose logs -f
```

### Конкретный сервис
```bash
docker-compose logs -f bot        # Логи бота
docker-compose logs -f backend    # Логи API
docker-compose logs -f frontend   # Логи фронтенда
```

### Последние 50 строк
```bash
docker-compose logs --tail 50 bot
```

---

## ❓ FAQ

### Q: Почему localhost:3000 не загружается?
**A**: Web App работает только внутри Telegram. Используйте эмуляцию (см. выше) или тестируйте через бота.

### Q: Почему кнопка "Открыть Web App" открывает браузер?
**A**: Telegram требует HTTPS для встроенного Web App. В разработке (HTTP) кнопка открывает внешний браузер.

### Q: Как протестировать полный функционал?
**A**:
1. Задеплойте на HTTPS (Vercel, Netlify, Railway)
2. Обновите `WEB_APP_URL` в `.env`
3. Перезапустите бота

### Q: Бот не отвечает на /start
**A**:
1. Проверьте логи: `docker-compose logs bot`
2. Убедитесь, что токен правильный в `.env`
3. Перезапустите бота: `docker-compose restart bot`

### Q: API возвращает 401 Unauthorized
**A**: Эндпоинты требуют заголовок `X-Telegram-Init-Data`. Используйте Swagger UI для тестирования без аутентификации.

---

## ✅ Чеклист для тестирования

- [ ] Бот отвечает на `/start`
- [ ] Бот отвечает на `/add`
- [ ] Бот отвечает на `/list`
- [ ] Бот добавляет желание по тексту
- [ ] Кнопки в боте работают
- [ ] API отвечает на http://localhost:8000/health
- [ ] Swagger UI открывается http://localhost:8000/docs
- [ ] Frontend открывается в Telegram (через кнопку бота)

---

## 🚀 Следующие шаги

1. ✅ Протестируйте бота в Telegram
2. ✅ Добавьте несколько желаний
3. ✅ Проверьте API через Swagger
4. 📦 Задеплойте на продакшн (HTTPS)
5. 🎉 Наслаждайтесь полным функционалом!

---

**Удачного тестирования! 🎯**
